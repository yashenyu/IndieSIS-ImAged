<UserControl x:Class="ImAged.MVVM.View.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImAged.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="525"/> <!-- LEFT -->
            <ColumnDefinition Width="15"/> <!-- SEPARATOR -->
            <ColumnDefinition/> <!-- RIGHT -->
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0"
                 Width="250" 
                 Height="50" HorizontalAlignment="Left" 
                 VerticalAlignment="Top" 
                 Margin="15,0,0,0"
                 Style="{StaticResource ModernTextbox}"/>

        <Button Background="{x:Null}" Grid.Column="0" Width="60" Height="45" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,195,0" BorderBrush="{x:Null}">
            <Border CornerRadius="10" Padding="8" Background="#252525" HorizontalAlignment="Center" VerticalAlignment="Top" Height="30">
                <TextBlock Text="Filter(3)" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Yu Gothic UI Semibold" Foreground="#FFC3C3C3"/>
            </Border>
        </Button>

        <!--LEFT SIDE CONTENT-->
        <StackPanel Grid.Column="0">
            
        </StackPanel>
        
        

        <!--RIGHT SIDE CONTENT-->
        <StackPanel Grid.Column="2" Background="#FF232323">
            <!-- Image Preview -->
            <Image Source="preview.jpg"
                       Width="200"
                       Height="120"
                       Stretch="UniformToFill"
                       Margin="0,0,0,20" />

            <!-- FILE NAME -->
            <TextBlock Text="File Name.ttl"
                           Foreground="White"
                           FontWeight="Bold"
                           FontSize="16"
                           Margin="10,0,0,20"/>

            <Separator Grid.Column="2" Background="#FF2B2A2A" Width="240" HorizontalAlignment="Left" Margin="10,2,0,2" ></Separator>

            <!-- FILE PARAMETERS -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Height="220" Margin="10,10,0,0">
                    <TextBlock Text="File Details" Foreground="White" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                    <TextBlock Text="Type: " Foreground="Gray"/>
                    <TextBlock Text="File Size: " Foreground="Gray"/>
                    <TextBlock Text="Location: " Foreground="Gray"/>
                    <TextBlock Text="Author: " Foreground="Gray"/>
                    <TextBlock Text="Modified: " Foreground="Gray"/>
                    <TextBlock Text="State: " Foreground="Gray"/>
                </StackPanel>


                <StackPanel Grid.Column="1" Width="90" Height="112" Margin="0,40,-6,78" HorizontalAlignment="Right">
                    <TextBlock Text="jpg" Foreground="Gray"/>
                    <TextBlock Text="n kb" Foreground="Gray"/>
                    <TextBlock Text="Work/User1" Foreground="Gray"/>
                    <TextBlock Text="User" Foreground="Gray"/>
                    <TextBlock Text="2025/18/16" Foreground="Gray"/>
                    <TextBlock Text="Active" Foreground="Gray"/>
                </StackPanel>
            </Grid>
        </StackPanel>

    </Grid>
</UserControl>