<UserControl x:Class="ImAged.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImAged.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             Background="Transparent">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Viewbox Grid.Row="0" Stretch="Uniform" MaxHeight="100" Margin="0,20,0,10">
            <TextBlock Text="INDIESIS"
                       FontSize="60"
                       Foreground="White"
                       FontFamily="Bahnschrift Light"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Viewbox>

        <Viewbox Grid.Row="1" Stretch="Uniform">
            <ItemsControl ItemsSource="{Binding Developers}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="2" Columns="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12"
                                Background="#171717"
                                Padding="16"
                                Margin="10"
                                SnapsToDevicePixels="True">
                            <StackPanel HorizontalAlignment="Center">
                                <Grid Width="200" Height="200">
                                    <Ellipse Fill="#171717"/>
                                    <Ellipse Stroke="#6C5CE7" StrokeThickness="2"/>
                                    <Image Source="{Binding ImagePath}" 
                                           Stretch="UniformToFill"
                                           RenderOptions.BitmapScalingMode="HighQuality"
                                           SnapsToDevicePixels="True">
                                        <Image.Clip>
                                            <EllipseGeometry Center="100,100" RadiusX="100" RadiusY="100"/>
                                        </Image.Clip>
                                    </Image>
                                </Grid>

                                <TextBlock Text="{Binding Name}" 
                                           Foreground="White" 
                                           FontSize="22" 
                                           FontWeight="SemiBold" 
                                           Margin="0,16,0,0" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Role}" 
                                           Foreground="#B3FFFFFF" 
                                           FontSize="14" 
                                           Margin="0,6,0,0" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding School}" 
                                           Foreground="#B3FFFFFF" 
                                           FontSize="14" 
                                           Margin="0,6,0,0" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Viewbox>
    </Grid>
</UserControl>
