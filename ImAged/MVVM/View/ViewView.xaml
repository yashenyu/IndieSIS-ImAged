<UserControl x:Class="ImAged.MVVM.View.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:ImAged.MVVM.ViewModel"
             xmlns:local="clr-namespace:ImAged.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             x:Name="Root"
             Background="#121212">

    <UserControl.Resources>
<<<<<<< Updated upstream
        <!-- The ViewModel is instantiated here and set as the DataContext -->
        <viewModel:MainViewModel x:Key="MainViewModel"/>

        <!-- Tab button style -->
=======
        <viewModel:MainViewModel x:Key="MainViewModel"/>

>>>>>>> Stashed changes
        <Style x:Key="TabRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Root"
                                Background="Transparent"
                                CornerRadius="16"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2D2D2D"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#262626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

<<<<<<< Updated upstream
        <!-- Search TextBox style -->
=======
>>>>>>> Stashed changes
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

<<<<<<< Updated upstream
        <!-- A simple converter to check if a value equals a parameter -->
=======
>>>>>>> Stashed changes
        <local:EqualsConverter x:Key="EqualsConverter"/>

    </UserControl.Resources>

<<<<<<< Updated upstream
    <!-- Set the DataContext for the entire Grid to your MainViewModel -->
=======
>>>>>>> Stashed changes
    <Grid Margin="20" DataContext="{StaticResource MainViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

<<<<<<< Updated upstream
        <!-- Search box (top-left) -->
=======
>>>>>>> Stashed changes
        <TextBox Grid.Row="0"
                 x:Name="SearchBox"
                 Width="300"
                 Height="36"
                 HorizontalAlignment="Left"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource SearchTextBoxStyle}"
<<<<<<< Updated upstream
<<<<<<< Updated upstream
                 Margin="850,23,0,48" Grid.RowSpan="2"/>
=======
                 Margin="841,23,0,48" Grid.RowSpan="2"/>
>>>>>>> Stashed changes

        <!-- Title and Separator -->
=======
                 Margin="841,23,0,48" Grid.RowSpan="2"/>

>>>>>>> Stashed changes
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock
                Text="Folders"
                FontWeight="Bold"
                Foreground="#FFEDEDED"
                Margin="0,0,0,6"
                FontSize="30" FontFamily="Yu Gothic UI" />
            <Separator
                Height="1"
                Foreground="#383838"
                Background="#FF2B2B2B"
                HorizontalAlignment="Stretch"/>
        </StackPanel>

<<<<<<< Updated upstream
        <!-- Tabs -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <!-- Bind to the ChangeTabCommand and pass the tab name as a parameter -->
=======
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
>>>>>>> Stashed changes
            <RadioButton GroupName="FolderTabs"
                         Content="Recent"
                         IsChecked="{Binding CurrentTab, Converter={StaticResource EqualsConverter}, ConverterParameter=Recent}"
                         Style="{StaticResource TabRadioButtonStyle}"
                         Command="{Binding ChangeTabCommand}"
                         CommandParameter="Recent"/>

            <RadioButton GroupName="FolderTabs"
                         Content="Archived"
                         IsChecked="{Binding CurrentTab, Converter={StaticResource EqualsConverter}, ConverterParameter=Archived}"
                         Style="{StaticResource TabRadioButtonStyle}"
                         Command="{Binding ChangeTabCommand}"
                         CommandParameter="Archived"/>
        </StackPanel>

<<<<<<< Updated upstream
        <!-- Folder grid -->
        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <!-- The ItemsControl is correctly bound to the Folders property -->
            <ItemsControl ItemsSource="{Binding Folders}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- To make the entire border clickable, use a Button. -->
                        <Button Background="Transparent" BorderBrush="Transparent" Padding="0" Margin="10" 
                                Command="{Binding FolderClickedCommand}">
                            <Border Width="260" Height="180"
                                    Background="#1E1E1E"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Thumbnail -->
                                    <Border Grid.Row="0" Margin="6" CornerRadius="6" ClipToBounds="True">
                                        <Image Source="{Binding Thumbnail}"
                                               Stretch="UniformToFill"/>
                                    </Border>

                                    <!-- Info -->
                                    <StackPanel Grid.Row="1" Margin="10,6,10,10">
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="White"
                                                   FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Info}"
                                                   Foreground="#9CA3AF"
                                                   FontSize="12"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
=======
        <ItemsControl Grid.Row="3" ItemsSource="{Binding Folders}" VirtualizingPanel.IsVirtualizingWhenGrouping="True" ScrollViewer.CanContentScroll="True">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.Template>
                <ControlTemplate TargetType="{x:Type ItemsControl}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsPresenter/>
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>

            <VirtualizingPanel.IsVirtualizing>True</VirtualizingPanel.IsVirtualizing>
            <VirtualizingPanel.VirtualizationMode>Recycling</VirtualizingPanel.VirtualizationMode>
            <ScrollViewer.IsDeferredScrollingEnabled>True</ScrollViewer.IsDeferredScrollingEnabled>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Background="Transparent" BorderBrush="Transparent" Padding="0" Margin="10"
                            Command="{Binding FolderClickedCommand}">
                        <Border Width="260" Height="180"
                                Background="#1E1E1E"
                                CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Margin="6" CornerRadius="6" ClipToBounds="True">
                                    <Image Source="{Binding Thumbnail}"
                                           Stretch="UniformToFill"/>
                                </Border>

                                <StackPanel Grid.Row="1" Margin="10,6,10,10">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="White"
                                               FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Info}"
                                               Foreground="#9CA3AF"
                                               FontSize="12"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
>>>>>>> Stashed changes
    </Grid>
</UserControl>